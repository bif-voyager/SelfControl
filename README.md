Простое десктопное приложение на Python для повышения продуктивности. Программа помогает сфокусироваться на учебе или работе, временно блокируя отвлекающие сайты и приложения.

#№ ✨ Возможности
* **Блокировка сайтов**: Модифицирует системный файл `hosts`, перенаправляя нежелательные сайты на `127.0.0.1`.
* **Блокировка приложений**: Автоматически закрывает процессы указанных программ (игры, мессенджеры).
* **Защита от закрытия**: Во время таймера кнопку закрытия окна ("крестик") нельзя нажать.
* **Таймер**: Устанавливается пользователем (в минутах). После истечения времени блокировка снимается автоматически.

## 🛠️ Технологии
* **Python 3**
* **Tkinter** — графический интерфейс.
* **psutil** — управление процессами (поиск и завершение программ).
* **threading** — работа таймера в фоновом режиме, чтобы интерфейс не зависал.

## 🚀 Установка и запуск

### 1. Требования
Программа работает **только на Windows** (так как используются специфичные пути к системным файлам).

Вам понадобится библиотека `psutil`. Установите её командой:
```bash
pip install psutil
